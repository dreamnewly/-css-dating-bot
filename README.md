# 💘 EvilCorp Dating App с AI Chat

Современное веб-приложение для знакомств с интеграцией OpenAI GPT для умных чат-ботов.

## ✨ Особенности

- 🤖 **AI-чат бот** на базе GPT-3.5-turbo
- 🌐 **Двуязычность**: Русский и Казахский языки
- 💬 **Контекстные диалоги**: Бот помнит историю беседы
- 🎨 **Современный UI/UX** с темной темой
- 🎁 **Система подарков** и супер-лайков
- 📍 **Фильтр по городам**
- ✅ **Верификация профиля**
- 🌙 **Темная/Светлая тема**

## 🚀 Быстрый старт

### Шаг 1: Клонируйте репозиторий

```bash
git clone https://github.com/ваш-username/dating-app.git
cd dating-app
```

### Шаг 2: Установите зависимости

```bash
npm install
```

### Шаг 3: Настройте API ключ

1. Скопируйте файл с примером:
```bash
cp .env.example .env
```

2. Откройте `.env` и добавьте свой OpenAI API ключ:
```env
OPENAI_API_KEY=sk-proj-ваш-настоящий-ключ-здесь
PORT=3000
```

3. Получите ключ на [platform.openai.com/api-keys](https://platform.openai.com/api-keys)

### Шаг 4: Запустите сервер

```bash
npm start
```

Вы увидите:
```
🚀 Прокси-сервер запущен на http://localhost:3000
📡 API доступен на http://localhost:3000/api/chat
```

### Шаг 5: Откройте приложение

Откройте файл `index.html` в браузере или используйте Live Server в VS Code.

## 📁 Структура проекта

```
dating-app/
├── index.html          # Основное приложение
├── server.js           # Прокси-сервер для OpenAI API
├── package.json        # Зависимости Node.js
├── .env.example        # Пример конфигурации
├── .gitignore          # Игнорируемые файлы
└── README.md           # Документация
```

## 🔧 Технологии

- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **Backend**: Node.js, Express
- **AI**: OpenAI GPT-3.5-turbo
- **Языки**: Русский, Казахский

## 🌍 Деплой

### Vercel (рекомендуется)

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Деплой:
```bash
vercel
```

3. Добавьте переменную окружения `OPENAI_API_KEY` в настройках проекта

### Railway

1. Создайте аккаунт на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Добавьте переменную `OPENAI_API_KEY`
4. Deploy автоматически запустится

### Heroku

```bash
heroku create your-app-name
heroku config:set OPENAI_API_KEY=ваш-ключ
git push heroku main
```

## 🔒 Безопасность

- ✅ API ключ хранится в `.env` (не в GitHub!)
- ✅ CORS настроен правильно
- ✅ Переменные окружения для продакшена
- ✅ `.gitignore` исключает чувствительные данные

## 🐛 Решение проблем

### Ошибка "Cannot find module"
```bash
npm install
```

### Ошибка "EADDRINUSE" (порт занят)
Измените порт в `.env`:
```env
PORT=3001
```

И в `index.html` обновите URL:
```javascript
const PROXY_URL = 'http://localhost:3001/api/chat';
```

### API не отвечает
- Проверьте, что сервер запущен (`npm start`)
- Убедитесь, что API ключ правильный в `.env`
- Проверьте баланс на OpenAI аккаунте

## 📝 Лицензия

MIT License - используйте свободно!

## 🤝 Вклад в проект

Pull requests приветствуются! Для больших изменений сначала откройте issue.

## 📧 Контакты

- GitHub: [@ваш-username](https://github.com/ваш-username)
- Email: your.email@example.com

---

Сделано с ❤️ и AI

